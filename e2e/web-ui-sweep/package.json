{
  "name": "mcpproxy-web-ui-sweep",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright Web UI sweep driven by scripts/run-web-smoke.sh (manual) and the release QA gate (advisory job)",
  "scripts": {
    "test": "playwright test",
    "install-browsers": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0"
  }
}
