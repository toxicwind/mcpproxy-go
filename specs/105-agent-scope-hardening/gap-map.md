# Spec 105 — Gap Map (synthesised from 8 per-FR reader results + adversarial verdicts, HEAD `b39800a89`)

**Headline.** 54 claimed gaps, **0 refuted outright**; 6 were *narrowed* by the verifier (kept, with the corrected claim below). Every roadmap task under Spec 105 except the five merged siblings (#1223 tier, #1224 tail_log, #1225 set_profile, #1226 read_cache, #1227 prompts) is `status: todo` at `roadmap.yaml:430-469` and the code confirms it. Cross-cutting: **16 existing tests pin the pre-105 behaviour and must be inverted, not deleted** (listed in §7).

Gap-id convention: `<FR>-G<n>` (reader ids were per-FR `G1..`; prefixed here to disambiguate).

---

## 1. Confirmed gaps

| id | FR | claim (verifier-corrected) | evidence (file:line) | test sketch | sev | roadmap task |
|---|---|---|---|---|---|---|
| FR005-G1 | FR-005 | Scope filter runs AFTER the BM25 top-K: `Search(query, limit)` cuts over the whole shared index, then `indexedToolVisible` drops hidden hits; hidden higher-scoring tools displace authorized ones (token-only restriction / profile-index-fallback path; per-profile index path unaffected). `total`, `filter_diagnostics`, `explain_tool` rank and `include_disabled` window inherit the truncated window. | `internal/server/mcp.go:1720` → `internal/index/manager.go:105-118` → `internal/index/bleve.go:274` (`Size=limit`); post-filter loop `mcp.go:1759-1799`; corpus selection `mcp.go:1709-1717` | createTestMCPProxyServer; servers a,b; index `a:rotate_keys` + two b tools with repeated query terms; ctx AllowedServers ["a"]; `{query:"rotate keys", limit:1}` → expect `[a:rotate_keys]`, HEAD `[]`/total 0; control `["*"]` → b tool. Verified by probe. | P1 | scope-retrieve-tools |
| FR005-G2 | FR-005 | `usage_summary.top_tools` (include_stats) is fleet-wide top-10 by count keyed `server:tool`, no scope filter, no index-presence check (stale/removed-tool history surfaces); per-entry `usage_count` is already scoped. Admin output must stay fleet-wide (SC-005). | `mcp.go:2037-2042` `GetToolStats(10)`; `internal/storage/manager.go:424-456` (cut before filter); increment sites `mcp.go:2674,3134` | Seed `IncrementToolUsage("b:secret_sentinel_tool")`×3, `a:echo`×1, `a:removed_tool`×1 (unindexed); a-only ctx → expect `[{a:echo,1}]`, no sentinel; admin unchanged; `TestRetrieveToolsFullMode_GoldenByteIdentity/include_stats` must still pass. Verified by overlay probe. | P1 | scope-retrieve-tools |
| FR005-G3 | FR-005 | `session_risk` computed over every Connected server in the fleet snapshot; hidden b with nil-annotation tool flips a-only token's level to `high`/`lethal_trifecta:true`. | `mcp.go:2007-2016`; `internal/server/mcp_annotations.go:29-40,98-104`; predicate exists unused at `mcp.go:1752-1754` | buildMCPProxyWithActivation; StateView a (readOnly tool) + b (nil annotations); a-only ctx → expect `low`/false; delete b → deep-equal; admin keeps `high`. | P1 | scope-retrieve-tools |
| FR005-G4 | FR-005 | `debug.total_indexed_tools` = root `p.index.GetDocumentCount()` for every caller, even when a profile index was searched. | `mcp.go:2021-2027`, `mcp.go:5517-5527`; `internal/index/manager.go:138-143` | Index a:t1, b:t2, b:t3; a-only ctx `{debug:true}` → expect 1, HEAD 3; admin 3. Hint: `server_name` facet term counts (`bleve.go:458-486`). | P2 | scope-retrieve-tools |
| FR005-G5 | FR-005/FR-013 | No US1.5 / retrieve-oracle test: nothing runs retrieve_tools with include_stats+debug+session_risk under an agent ctx against 3-server vs a-only fixtures. `TestRetrieveTools_ExcludesDisabledToolsPreRanking` (2 tools, limit 10) passes vacuously. | `mcp_visibility_test.go:96,150` (name-set only, limit never bites); `mcp_blocked_tools_test.go:38`; `mcp_session_risk_test.go:23` (no auth) | `TestRetrieveTools_ScopeOracle` table-driven over both fixtures; session_risk needs `buildMCPProxyWithActivation` (emitted only when `p.mainServer.runtime != nil`, `mcp.go:2010`). | P2 | scope-retrieve-tools |
| FR008-G1 | FR-008/US3.1 | Forward origin flip at the SetTools→publishDirectCatalog seam: a-only token's tools/list returns the NEW owner's full definition (`[a__b] …` description, new schema, new annotations; deferred: new signature suffix) because the filter resolves by name through the OLD catalog and appends the REGISTRY tool. Rendered `mcp.Tool` carries no identity (Meta nil). | `internal/server/mcp_routing.go:1142→1146`, `:184` (render), `:249-291/:307-311` (no Meta); `internal/server/mcp_direct_scope.go:59,97,101`; `mcp_direct_catalog.go:388,405`; leak pinned by `mcp_direct_skew_test.go:316-318` | rebuildPaused old {a,b__c,S1} → new {a__b,c,S2}; a-only listed() during pause → assert `a__b__c` absent (or desc !contains `a__b`, schema==S1); both modes; `*` + admin controls. Verified overlay, both modes fail. | P1 | scope-direct-publication |
| FR008-G2 | FR-008/US3.2 | Unparseable new display names (`__a__review`, `b__`) miss the old catalog in the seam and fall to the parse-failure→`directResolveBuiltin` rule; both filters pass built-ins through unconditionally, so an a-only token receives `__a`'s definition for the window. `builtinDirectToolNames` is empty; built-ins inferred, not positively identified. describe/dispatch not exposed. `mcp_direct_catalog_publish_test.go:95-101` codifies inference (asserts `retrieve_tools` → Builtin). | `mcp_direct_catalog.go:374,405,416-418`; `mcp_routing.go:64-70`; `mcp_direct_scope.go:62-65`; `mcp_direct_callability.go:66-70` | rebuildPaused adding `__a`/review with sentinel; assert not listed for a-only during seam; positive-identification test: `describe_tool` Builtin via explicit set, `__x__y`/`retrieve_tools` NOT Builtin. Verified. | P1 | scope-direct-publication |
| FR008-G3 | FR-008/US3.2 | Availability-only: for the seam width, an agent authorized for the new tool's owner is withheld it — plain addition (`b__x` for b-only token, dropped as Denied because absent from old catalog; `TestSkew_AddedNameBeforeItsCatalogEntry` pins this) and reverse flip a__b→a (a-only refused via stale owner `a__b`, not absence). `*` agent NOT affected. | `mcp_direct_scope.go:59-71,97-130`; `mcp_direct_callability.go:67-84`; `mcp_direct_skew_test.go:195-224` | Reverse-flip + plain-addition fixtures, assert listed during window. Verified. | P2 | scope-direct-publication |
| FR008-G4 | FR-008/US3.3 | Same-owner tier change read→destructive: `{read}` token still LISTS the tool during the seam (tier read from stale catalog `RequiredPermission`) and receives the new destructive definition; dispatch already refuses. `mcp_direct_skew_test.go:475-477` documents it as residual. | `mcp_direct_scope.go:133-141`; `mcp_routing.go:1143-1147`; handler correct at `mcp_routing.go:391-393,434-445` | rebuildPaused ReadOnly→Destructive; `{read}` token not listed; handler IsError "Permission denied"; full+deferred. Verified both fail. | P2 | scope-direct-publication |
| FR008-G5 ≡ FR010-G5 | FR-008/US3.4/FR-010 | Direct handler scope refusal names the captured canonical owner (`Access denied: token does not have access to server 'a__b'`; profile: `server '%s' is not in profile '%s'`). Reachable only inside the seam (steady-state mcp-go call-time filter answers `tool '<name>' not found`). Test pins the leak. | `mcp_routing.go:424` (profile), `:434` (token); `mcp_direct_skew_test.go:328`; mcp-go `server/server.go:2080-2086` | Inside rebuildPaused call registeredHandler(`a__b__c`) as a-only → text !contains `a__b`, equals not-found shape. Message parity only (handler err → -32603 vs filter -32602). | P2 | scope-direct-publication (owner) / cited by scope-refusal-shapes |
| FR008-G6 | FR-008/SC-003/FR-013 | `__a`-server fixtures missing (no test constructs ServerName `__a` for a tool; positive built-in identification test missing). Steady-state sketch PASSES on HEAD (catalog-first resolution works); the defect the fixture must pin is the **seam** (= G2 mechanism: Builtin classification → listed with `[__a] …` + schema; tools/call returns isError naming `__a`, differing from -32602 not-found). | `mcp_direct_catalog.go:151-224,416-418`; `mcp_routing.go:65-66`; `mcp_direct_scope.go:63-67`; `mcp_direct_callability.go:70-74` | Steady-state test (listed/describable/dispatchable for `*`+admin; withheld + -32602 parity for a-only) PLUS rebuildPaused added-name seam variant, both modes. | P2 | scope-direct-publication |
| FR008-G7 | FR-008/SC-003/SC-005 | Empty raw tool name (`a__`) is admitted to the catalog, rendered, listed/describable/dispatchable for admins and a-authorized agents; spec (SC-003, Edge Case (3) `spec.md:107`, SC-005, `checklists/requirements.md:46`) withholds no-identity definitions from every caller. Tests pin admission. Prompt-side counterpart (`buildAggregatedServerPrompts` admits `""`) also unimplemented. Retrieve dispatch already rejects empty tool name (`mcp.go:2256`). Needs one-line spec clarification naming the tool analogue. | `mcp_direct_catalog.go:169,213-224`; `mcp_routing.go:159-231,390-441,1322-1326`; `mcp_direct_scope.go:40-51,115-140`; `mcp_direct_catalog_test.go:177-235` | `buildDirectCatalog([{a,""}]).Len()==0`; filter omits `a__` for admin ctx and a-agent; record admin outcome. Verified fails (Len 1). | P2 | scope-direct-publication |
| FR010-G1 | FR-010/US1.8 | No-live-client branch enumerates **every** `Manager.clients` key into `Available servers: [b, a__b]` for a scoped token (target `a` cannot appear — its absence is the branch precondition; order nondeterministic). Reachable when target is in storage+StateView but has no client. | `mcp.go:2473-2495` (`GetAllServerNames()` :2476); `internal/upstream/manager.go:966-975`; reachability `mcp_call_tool_target_tier_test.go:152,253` | Fixture A: a (no client), b/a__b via AddServerConfig after PhaseReady; token [a]; `call_tool_read a:t` → text identical across fixtures, no sentinel (order-insensitive). One-liner fix: filter through `serverInScope` / drop list for non-admin. | P1 | scope-refusal-shapes |
| FR010-G2 | FR-010 | describe_tool (indexed, definition mode) evaluates not-found over the FULL index: hidden exact `b:read` → `visReasonServerNotInScope` → plain remediation; nonexistent `b:read` with authorized `B:read` → "did you mean 'B:read'?" — existence oracle. Check mode already one-policy. | `internal/server/mcp_visibility.go:51-60` (index presence before scope), `:224-248`; `internal/server/mcp_describe_tool.go:151-159,223-238` | Index B:read (+hidden b:read in fixture A); token [B]; describe `b:read` → equal responses with suggestion. Verified probe fails. Keep `describe_plain_corpus_test.go` bytes (gitlab has no case-variant). | P1 | scope-refusal-shapes |
| FR010-G3 | FR-010 | Direct-surface alias admission is fleet-wide at catalog build: canonical `x__y:z` (server x__y, tool z) withdrawn because hidden x's tool `y:z` displays as `x__y:z`; `Lookup` hits the hidden display entry, visibility gate fails → not_found in definition AND check mode; disappears when hidden x is absent. Admin control (`TestDescribeDirect_DisplayAndCanonicalNamespaceOverlap`) must stay green. | `internal/server/mcp_direct_catalog.go:151-157,231-244,258-269`; `mcp_routing.go:126`; `internal/server/mcp_describe_direct.go:75-89,250-253,303-305`; `mcp_direct_scope.go:124-132` | Catalog A {x:y:z sentinel, x__y:z} vs B {x__y:z}; token [x__y]; describe `x__y:z` definition + check → identical (definition / ready), no sentinel; admin not_found unchanged. Verified. | P1 | scope-refusal-shapes |
| FR010-G4 | FR-010 | Direct-surface case-correction `return "", false` on the FIRST case-fold match that is invisible instead of `continue` (indexed sibling `suggestCanonicalToolID` continues). Definition mode only; check mode unaffected. One-word fix at `:151`. | `mcp_describe_direct.go:133-155` (:150-152); contrast `mcp_visibility.go:238-243`; sort `mcp_direct_catalog.go:274` | Catalog A hidden `B:read` + authorized `b:Read`; token [b]; describe `b:read` → suggestion `b:Read` in both fixtures. | P2 | scope-refusal-shapes |
| FR010-G6 | FR-010(2) | Over-tier tool on an AUTHORIZED server is refused `-32602 tool '<name>' not found` on the HTTP direct surface because `directEntryInScope` (scope+tier in one predicate) is a `WithToolFilter` that mcp-go v1.0.0 re-runs at tools/call; the handler's "Permission denied" branch is unreachable for agent tokens outside the seam. All existing direct tier tests call the handler directly. Same mechanism makes pending/changed/disabled → not-found (spec (3) "unchanged", leave/document). | `mcp_direct_scope.go:40-100,139-141`; `mcp_routing.go:958-961`; handler `:456-465`; mcp-go `server.go:1972-1990,2077-2086` | Real proxy; ctx [a],{read}; `p.directServer.HandleMessage` tools/call `a__write_tool` → expect isError "Permission denied: … 'write'", zero upstream; HEAD -32602. Verified. Fix: keep SCOPE in the filter, move tier/callability to a list-only path. | P2 | scope-refusal-shapes |
| FR010-G7 | FR-010(1)/US1.8 | With a profile in effect (pin/URL/session), profile gate runs before token gate on call_tool_* and in the sandbox (allow-list = profile set, not profile∩token): server in profile but outside AllowedServers → token text/`access_denied`; nonexistent → `server 'zzz' is not in profile 'P'`/`server_not_allowed`. Discloses b ∈ P; SC-001 differential fails (a-only fixture's `EffectiveServers` drops b). No profile → already identical. Direct surface masked by call-time filter outside seam. | `mcp.go:2262-2274` vs `:2287-2293`; `profile_resolver.go:99-115,132-141`; `internal/config/profiles.go:101-118`; `mcp_code_execution.go:249-256,1198-1229`; `internal/jsruntime/runtime.go:384-411`; `internal/httpapi/tokens.go:174-186,437-455` | Token {[a], pin P={a,b}}; `b:t` vs `zzz:t` → identical text; nested `call_tool('b',..)` vs `('zzz',..)` → identical envelope. Compute effective scope once (profile∩token), one body, agent-token only (admins on /mcp/p keep today's text). | P2 | scope-refusal-shapes |
| FR003-G1 | FR-004/FR-003 | `/mcp/p/<unknown>` 404 body enumerates EVERY configured profile in `available` for any unpinned caller incl. restricted agent tokens; only gate is `pin==""`. `selectableProfileNames` exists (#1225) but is only called from set_profile. | `internal/server/server.go:2360-2373`, mount `:2703-2705`; predicate `internal/server/profile_tool.go:188-208` (sole caller `:89`) | mintScopedToken("a-only","research-srv"); POST initialize `/mcp/p/nonexistent` → no `available` (or ⊆ selectable). | P1 | scope-selectable-profile-predicate |
| FR003-G2 | FR-004 | No selectable predicate on the profile URL: unpinned token restricted to research-srv initializes 200 through `/mcp/p/deploy` (disjoint), empty profile, empty grant; missing/deleted → 404 with full enumeration; `/mcp/p`,`/mcp/p/` same. | `server.go:2320-2384` (`:2380-2383` no intersection); `mcpAuthMiddleware :348-462` | a-only unpinned token: `/mcp/p/deploy` vs `/mcp/p/nonexistent` → equal status+body (slug-normalised); delete deploy → byte-identical; `/mcp/p/research` positive control. Verified 200 vs 404. | P1 | scope-selectable-profile-predicate |
| FR003-G3 | FR-004 | Pin mismatch = 403 body naming pin+slug; missing/deleted pin = 404 `unknown profile '<slug>'`; no-profiles = 404 distinct body evaluated BEFORE the pin check. FR-003 set_profile parity already shipped; its pin-mismatch text (`profile_tool.go:65-67`) not required by FR-003 wording. | `server.go:2326-2332,2343-2350,2361-2376`; locking tests `profile_integration_test.go:667-670,681-701,201-233,772-799` | Pinned "research": `/mcp/p/deploy`, `/mcp/p/nope`, deleted-pin `/mcp/p/research` → identical status+body. HEAD 403/403/404. | P1 | scope-selectable-profile-predicate |
| FR003-G4 | FR-004 | `no profiles configured` 404 runs before pin check → deleted-pin token distinguishes empty fleet from non-empty; pinned mismatched slug gets 404 no-profiles (empty) vs 403 (non-empty). #1227 R2-4 declined as pre-existing. | `server.go:2324-2332` before `:2343`; `TestProfile_404NoProfiles :201-216` unauth only | Fleet A=[deploy] vs B=nil; pinned research: `/mcp/p/research`, `/mcp/p/deploy` → equal across A/B per slug. | P1 | scope-selectable-profile-predicate |
| FR003-G5 | FR-004 | `/mcp/p` and `/mcp/p/` (slug `""`): unpinned → 404 `unknown profile ''` + full `available`; pinned → 403 `cannot access profile ''`; no test requests either path. Broader: no selectability check at all on existing profiles (= G2). | `server.go:2335-2337,2343-2350,2354-2376,2704-2705`; grep `"/mcp/p"` in tests = 0 | For a-only unpinned and research-pinned: `/mcp/p`, `/mcp/p/`, `/mcp/p/nonexistent` → shared status+body, no `available`. | P1 | scope-selectable-profile-predicate |
| FR003-G6 | FR-003 (URL precedence) | set_profile on `/mcp/p/<slug>` reports selected-profile∩token (or all∩token on clear) in `servers`, ignoring the URL `ProfileScope` that governs subsequent requests. | `profile_tool.go:82,108` (no `ProfileScopeFromContext`/`resolveActiveProfile` on unpinned paths); `profile_resolver.go:145-147`; `server.go:2378-2383` | ctx = scoped [research-srv,deploy-srv] + `profile.WithProfileScope(research)`; set_profile "deploy" → active "deploy", servers [research-srv]; clear → [research-srv]. Verified fails. | P2 | scope-selectable-profile-predicate |
| FR003-G7 | FR-003 | Pinned token clearing selection stores `""` but reports the pin name (also stale pin) as `active_profile`. Locked by two tests + `docs/features/profiles.md:70`; #1225 F3 follow-up. | `profile_tool.go:72,79-80`; `profile_resolver.go:132-141`; `profile_pin_enforcement_test.go:150,160`; `profile_tool_test.go:356` | pinnedProxy, pin research, set_profile "" → `active_profile==""`, servers [research-srv]; flip tests + docs line together. | P2 | scope-selectable-profile-predicate |
| FR003-G8 (narrowed) | FR-004 (decision) | Pinned token whose configured pin has zero reach (empty/ghost profile, disjoint AllowedServers) is admitted by set_profile `<pin>` and initializes 200 through `/mcp/p/<pin>`, while a deleted pin is refused → token observes whether its own pin exists. **Not spec-literal**: FR-003/004 predicate + Edge Case (1) are worded for UNPINNED tokens; Definitions keep "deleted pin → deny-all as today"; #1225 F2 locked "configured pin always selectable" (`TestHandleSetProfile_PinnedTokenSelectsDisjointPin`). Open design decision — record in spec; under zero-interruption rule pick spec-literal FR-004 text (refuse) and document. | `profile_tool.go:189-196` (pin branch, no reach check) vs `:201-207`; `server.go:2343-2350,2377-2380`; `profile_tool_test.go:393-411` | If refuse: pinned "empty"/["*"] set_profile "empty" → same text as deleted-pin refusal, no mutation; HTTP `/mcp/p/empty` → uniform refusal. | P2 | scope-selectable-profile-predicate |
| FR001-G1 | FR-002/SC-004/SC-005 | Nil-producer (legacy, anything via `Store`) entries redeemable by admin/admin_user/anonymous (nil → `CallerKindAnonymous`), never invalidated on ANY refused redemption (agent refusal keeps entry too), survive DB reopen. Anonymous = spec grey zone (`spec.md:36`), admin/admin_user unambiguous. `authorization_test.go:104-110` pins the opposite. | `internal/cache/manager.go:231-242,164-214`; `internal/cache/authorization.go:77-95`; only call site `mcp.go:5658-5659`; REST via `mcp.go:6171-6177` | Store legacy; every reader kind → `ErrUnauthorizedRead`; `Peek` false; close+reopen bbolt → still absent; server-level admin handleReadCache → IsError, no `records`. Verified all pass-through on HEAD. | P1 | scope-cache-legacy-invalidation |
| FR001-G2 | FR-002/SC-005 | Internal `registry-servers:<id>:<tag>:<query>:<limit>` entries fully redeemable by admins via read_cache; `npm:<pkg>` guesser entries existence-probeable (object payload → "no records array found", distinct from not-found). Guessable keys + distinct agent refusal text (`not readable with this credential` vs `cache key not found`) = probe oracle for agents. Must refuse WITHOUT evicting (Peek/Get readers at `runtime.go:2160`, `guesser.go:258`). | `internal/runtime/runtime.go:2131-2133,2226`; `internal/experiments/guesser.go:257-258,349`; `manager.go:108-113,231-241,494-513`; `mcp.go:5657-5665` | Store registry key; admin readCacheAs → IsError, no "srv"; agent live vs absent key → identical text; entry still Peek-able. Verified. | P1 | scope-cache-legacy-invalidation |
| FR001-G3 | FR-002 (Durable invalidation) | Expiry branch `Delete` then non-nil return inside `db.Update` → bbolt rollback; in-memory stats drift (TotalEntries−1, EvictedCount+1 in memory only). Guard branch has the same shape so refuse+invalidate added there would roll back. `TestExpiredRecords` never asserts absence. | `manager.go:168,183-190,192-196`; `manager_test.go:271-322`; `Invalidate` `:303` never called from server | Store; rewrite ExpiresAt past; Get errs; Peek → expect false (HEAD true); on-disk count == in-memory. Verified. | P2 | scope-cache-legacy-invalidation |
| FR001-G4 | FR-001 (monotone provenance) | Recursive child page stamped with the REDEEMER (`cacheStoreAs(reader)`), not the parent; `ReadCacheResponse` carries no Producer. Over-refusal only (child ⊇ parent always). Spec's named fixture does not bite on HEAD (refused for two other reasons incl. G6); discriminating test = parent producer W must read child K2 after an unscoped admin pages it. | `mcp.go:5658,5682-5690`; `internal/server/cache_authz.go:69-71,81-86`; `internal/cache/models.go:40-43`; `authorization.go:65-99` | Agent W ["*"] produces K1; admin pages K1 → K2; W reads K2 → expect success, HEAD "not readable". Plumb parent Producer via `ReadCacheResponse` (`json:"-"`). | P2 | scope-cache-legacy-invalidation |
| FR001-G5 | FR-001/FR-010(1) | read_cache refusal not non-disclosing: four distinguishable bodies (unauthorized / not found / expired / corrupt) on MCP and REST (status parity holds: isError / HTTP 500); timing class also differs. Tests pin the disclosing string. | `mcp.go:5658-5666`; `manager.go:170-174,186-190`; `mcp.go:6208-6219`; `server.go:3667-3669`; `internal/httpapi/server.go:5582-5583`; `mcp_read_cache_authz_test.go:86,171`; `mcp_call_tool_direct_test.go:32` | Narrow ctx [weather]; live broad key vs nonexistent → equal text on handleReadCache and CallToolDirect; keep `cache key not found` substring (admins unaffected). | P2 | scope-cache-legacy-invalidation |
| FR001-G6 | FR-001 (kind-first) / SC-005 | Profile checks run before `reader.Unrestricted()` → admin bound to URL/session profile refused unscoped or wider entries; any ProfileScoped reader kind affected. `authorization_test.go:65,68,72` codify it; #1226 R1-F2 declined. Keep deny-all guard ahead of the admin shortcut (codex assessment). | `authorization.go:78-89` before `:90-95`; `cache_authz.go:54-58`; `profile_resolver.go:149-160` | Table: producer admin unscoped, reader admin+ProfileScoped[a] → true (HEAD false); wider-profile admin → true; pinned wildcard agent vs unpinned agent entry → false (retained). Verified. | P2 | scope-cache-legacy-invalidation |
| FR001-G7 | FR-001/FR-002/FR-013 | Mandatory fixtures absent: upgrade fixture (pre-feature record → refused, absent after reopen, no content); fresh internal-entry (SC-005); recursive-child on MCP+REST; pinned-token REST (direct dispatch vs cache redemption); cross-session; upstream-content-block producer (`mcp.go:2711/3157`); held-call narrowing. **Held-call fixtures would PASS today** (producer captured at dispatch `mcp.go:2263-2268,2938-2940`) — fixture-only. Pinned-REST refusal holds but body-parity assertion fails. | `mcp_read_cache_authz_test.go` (3 tests, line 167 mutates after production); `authorization_test.go:93-121`; `manager_test.go:14-24` (no reopen); `mcp_call_tool_direct_test.go:35-48` seeds unstamped | (a) write Record JSON w/o `producer` into bucket, NewManager, GetRecordsAs(admin) refused, close/reopen, Peek absent; (b) held-call via blocking fake upstream (passes—keep as regression); (c) pinned REST: `CallToolDirect(call_tool_read b:x)` refused, `read_cache K_containing_b` refused with nonexistent-key body. | P2 | scope-cache-legacy-invalidation |
| FR007-G1 | FR-007 | Names differing only in non-`[A-Za-z0-9._-]` runes (`a/b`,`a_b`) share `server-a_b.log`; `ReadUpstreamServerLogTail` returns last N lines unfiltered; `handleTailLog` gates only the requested name. Records already stamped `{"server": "<raw>"}` → missing filter, not missing identity. Case-only (`A`/`a`) collides only on case-insensitive FS. | `internal/logs/logger.go:321-332,385,504-547`; `mcp.go:5741-5744,5787,5797`; `mcp_visibility.go:161-166` | internal/logs: writers a/b, a_b; sentinel in a/b; tail(a_b,50) → no sentinel (HEAD returns it, verified). Server-level a_b-only token, SC-001 differential with/without a/b. | P1 | scope-log-attribution |
| FR007-G2 | FR-007 | Unattributed/legacy lines returned to scoped callers; policy not co-owner-independent. `mcp_tail_log_scope_test.go:63-64,110-117` and `mcp_secret_redaction_test.go:288` REQUIRE plain unstamped canary served to scoped token. | `logger.go:527-547`; `mcp.go:5792-5801`; fixture `mcp_tail_log_scope_test.go:63-64` | O_APPEND `LEGACY_PLAIN_LINE`; attributed read excludes; admin whole-file includes. | P2 | scope-log-attribution |
| FR007-G3 | FR-007 | Tail limit precedes ownership filter; `lines_returned` = raw slice length; interleaved foreign line displaces an authorized one. Probe also exposed **shared-file write corruption** (two lumberjack sinks, independent offsets → torn fragments = non-attributable). | `logger.go:530-547`; `mcp.go:5787,5796,5803`; `mcp_server_view.go:117-123` | own1/foreign1/own2/foreign2; attributed tail(a_b,2) == [own1,own2]; server-level `lines_returned==2`, no foreign. Verified (HEAD returned torn fragment + foreign2). | P1 | scope-log-attribution |
| FR007-G4 | FR-007 (subject-bound OAuth) | `markOAuthComplete → StopCallbackServer(name)` (nil logger) → `adoptLoggerLocked(nil)` returns LAST-installed manager logger (a tee into that server's file); `stopCallbackServerLocked` logs target's name/bind_host/port/dropped-waiters through it, ignoring subject-bound `server.logger` (`config.go:1407`). Reachable to agents because tail_log is name-gated only. | `internal/upstream/core/connection_oauth.go:1705`; `internal/oauth/config.go:100-119,805,1023-1028,1303,1407,1686-1696,1701-1735`; `core/client.go:936-944` | Package-internal: two observer loggers, start a,b, `StopCallbackServer("a")` → a's obs has stop record, b's has none; reverse order. Verified fails (record landed in b). Fix: route stop through `server.logger`, do not call adoptLoggerLocked on stop. | P1 | scope-log-attribution |
| FR007-G5 | FR-007 (container ownership) | On EVERY Docker connect `ensureNoExistingContainers` lists `--filter name=mcpproxy-a-` with **no Go-side predicate**, logs foreign id+name into a's log, `docker rm -f`s foreign live containers (`a-b`, `a/b` both sanitize to `a-b`). Disconnect path `killDockerContainersByNamePatternWithContext` (HasPrefix) only in fallback when containerID & containerName empty. Label `com.mcpproxy.server=<raw>` exists but only used by fleet shutdown sweep. | `internal/upstream/core/docker.go:349-431 (:383,:406-408,:412-416), :501-582 (:511,:555-558,:562,:572-574)`; callers `connection_stdio.go:116`, `connection_launcher.go:66`; fallback `connection_lifecycle.go:302-316`; label `instance.go:57-65`, consumer `manager.go:754-755`; `isolation.go:661-669`; `dockernaming/naming.go:38-63` | Fake docker (SetWellKnownDockerPathsForTest + ResetDockerPathCacheForTest) `ps` → `deadbeef1234 mcpproxy-a-b-wxyz`; server a; assert no rm/stop/kill of foreign id, no log entry; `mcpproxy-a-wxyz` IS removed. Unit matcher `ownsContainer(server, name, labels)` table a vs a-b vs a/b vs A. | P1 | scope-log-attribution |
| FR007-G6 | FR-007/FR-013/SC-005 | No fixture for any named case (a/b vs a_b, case-only, interleaved ± co-owner, forced rotation, OAuth both orders, a vs a-b cleanup, admin outcomes). Case-only fixture must branch on FS case sensitivity (Linux CI = two files). | `logger_test.go:17-90`; `e2e_test.go:194`; `mcp_tail_log_scope_test.go:63-64,114`; `redirect_uri_followup_test.go:102-135`; no docker cleanup test | Names: `TestReadUpstreamServerLogTail_AttributedOnly_{CollidingNames,CaseOnlyNames,InterleavedFilterBeforeLimit,LegacyUnattributedWithheld,ForcedRotationSharedHistory}`, `TestTailLog_CollidingLogFile_*`, `TestCallbackStop_UsesRecordedServerLogger_{AB,BA}Order`, `TestDockerCleanup_MatchesOnlyCanonicalOwner`. Rotation = retained effect (MaxSize 1MB). | P2 | scope-log-attribution |
| FR009-G1 | FR-009 | `checkToolApprovals` keys records by `extractToolName` (first-colon strip) → `ns:erase` never gets its own record. With approved `erase` baseline and identical desc+schema (**annotations excluded from hash** — destructive `ns:erase` also inherits), `ns:erase` silently inherits approval, indexed, callable with zero review. Differing desc/schema → `erase` marked `changed` (false rug-pull), both blocked, `ns:erase` un-approvable (`ApproveTools` exact-key). | `internal/runtime/tool_quarantine.go:36-55,443,456,459,570-583,596,606-616,813,1216-1229`; `internal/runtime/lifecycle.go:975-980`; consumer fallback `tool_gate.go:120-139,220-221` | setupQuarantineRuntime manual trust; baseline erase approved; discover [erase, ns:erase] → expect `BlockedTools["ns:erase"]`, pending under own name; server-level full-tier a-only `call_tool_read a:ns:erase` → TOOL_QUARANTINED, zero upstream. Verified overlay (pending=0, not found, ApproveTools no-op). | P1 | scope-target-identity-producers |
| FR009-G2 | FR-009/SC-005 | Index collapse in THREE places: `bleve.go` docID `SplitN(Name,":",2)[1]` → `erase`/`ns:erase` share `a:erase` (last writer wins); `newToolsMap` collapses both to `erase`; old-key (`ns:erase` from CanonicalToolName) vs new-key mismatch reports `ns:erase` removed every rediscovery → `DeleteToolApproval("a","ns:erase")` deletes the exact-name Disabled toggle record → full-tier token dispatches after next discovery. | `internal/index/bleve.go:158-161,183,356-372,433`; `lifecycle.go:699-706,708-716,729-733,761-790 (:783)`; `internal/storage/bbolt.go:458-464`; toggle `tool_quarantine.go:1311-1322` | applyDifferentialToolUpdate [erase, ns:erase] → `GetToolsByServer` len 2 (HEAD 1); SaveToolApproval(ns:erase Disabled); rerun → record still present (HEAD ErrToolApprovalNotFound). Verified by execution. | P1 | scope-target-identity-producers |
| FR009-G3 | FR-009 | Direct-name dispatch and preflight read the EXACT approval key only (not `lookupToolApproval`) and treat no-record as Ready; a collapsed pending/Disabled record refuses `a:ns:erase` on retrieve/nested but admits `a__ns:erase` on `/mcp/all` and reports `ready` in preflight. Even with exact producers, no-record→ready must also change. | `internal/server/mcp_direct_callability.go:136,192-201,253`; `internal/server/preflight_glue.go:400-411`; `internal/preflight/classify.go:98-99`, `evaluator.go:272`; `mcp_routing.go:467` | Seed {a,erase,pending}, delete ns:erase; full-tier ctx: variant refused (passes), `makeDirectModeHandler` entry ns:erase → expect IsError/zero upstream (HEAD dispatched), `preflightApprovalReader.ToolApproval` → non-nil pending (HEAD nil). Verified. | P2 | scope-target-identity-producers |
| FR009-G4 | FR-009/US2.5/SC-002 | Failed identity resolution (undiscovered tool) → grantable `destructive` tier; scoped `{read,write,destructive}` token passes tier gate on retrieve and nested and reaches upstream; no freshness/generation check (found = name equality). Direct exempt (handler = registration identity). Two #1223 tests pin it; A14 live showed upstream executed. spec.md:107 says exception (2) applies to admins too (broader than "scoped callers"). | `mcp_code_execution.go:1260-1263,257,986-1000,742`; `mcp.go:2283,2317-2321,2389,2437,2510,6797-6799`; `jsruntime/runtime.go:401-409`; `contracts/intent.go:169-172`; `mcp_call_tool_target_tier_test.go:210-223,241-250` | Counting upstream [erase]; full-tier [a]; `call_tool_read a:ghost` → Permission denied, zero upstream (HEAD reaches upstream "tool not found"); script `call_tool('a','ghost')` → PERMISSION_DENIED envelope. Invert the two tests. Keep admin/unknown-server fail-open. | P2 | scope-target-identity-producers |
| FR009-G5 | FR-009 | "No record found" = implicit-approved on all three gate sites; `ClassifyTool` returns Ready on nil Approval before consulting the quarantine gate; collapsed-only approved `erase` record handed to `ns:erase`. Acknowledged todo. Cell 1 reachable in prod only via fail-open paths (`tool_quarantine.go:583-589`, `lifecycle.go:674-679`); cell 2 is live pre-migration state — exact-only reader must ship WITH exact producers (ApproveTools reads exact key). Gate new rule on `quarantine_enabled && !IsQuarantineSkipped()`. | `tool_gate.go:120-126,140-151,218-221`; `classify.go:98-100`; `mcp.go:2389-2427`; `mcp_direct_callability.go:197-201`; `preflight_glue.go:401-409` | Manual-trust server, quarantine on, StateView has ns:erase, storage no record → refused; seed only {a,erase,approved} → `a:ns:erase` refused. | P2 | scope-target-identity-producers |
| FR009-G6 | FR-009/SC-002/SC-005 | Acceptance tables (a)/(b) absent: no 54-cell retrieve matrix, no `{read,destructive}` row (0 fixtures repo-wide), no same-tier config-denied pair via `disabled_tools` on any path, no direct/nested matrix with zero-upstream oracle (direct cells use bare `&MCPProxyServer{}` + panic oracle; nested uses stub ToolAnnotationFunc), no US2.6 envelope through real runtime, no real-discovery producer fixture (would FAIL on HEAD), no SC-005 admin controls / erase+ns:erase-survive-indexing fixture. Counting oracle only covers 2 strict-off cells + `a:a:ns:erase` collision cells. | `mcp_call_tool_target_tier_test.go:78-207,414-472,1148-1185`; `mcp_routing_test.go:306-447`; `jsruntime/runtime_test.go:648-676`; `mcp.go:6534` → `tool_quarantine.go:1753` → `config.go:2179-2194` | Table-driven per path; pre-classify permissionDisallowed → intentMismatch → allowed; counting upstream; add disabled_tools=[ns:erase] cells incl. `auth.AdminContext()`; producer fixture via setupQuarantineRuntime. | P2 | scope-target-identity-producers |
| FR009-G7 (narrowed) | FR-009/FR-012 (docs only) | **Runtime is correct on HEAD** (exact-match `HasPermission`; `{read,destructive}` IS refused for write targets on all paths). Remaining gap is docs: `agent-tokens.md:140-150` claims "destructive implies both" but nothing expands cumulatively (CLI stores list verbatim; ValidatePermissions requires only `read`); target-tool tier requirement, annotation-less→read default, undiscovered→destructive are undocumented. #1223 follow-up item 171 is misworded. | `docs/features/agent-tokens.md:144-150`; `cmd/mcpproxy/token_cmd.go:164`; `internal/auth/agent_token.go:164-181`; `context.go:101-111`; `mcp.go:2317`; `mcp_routing.go:444-450`; `intent.go:223-227` | Docs paragraph "Target tool tier"; optional regression cell `{read,destructive}`×write_thing (passes today). | P2 | scope-target-identity-producers |
| FR01x-G1 | FR-012 (Edge Cases :116) | Missing stored-script name returns `Available scripts (N): …` + total to agent-token callers on `/mcp` (code exec on) and `/mcp/code`; no caller-kind branch anywhere; only test uses nil AuthContext. REST also enumerates but is out of spec scope. | `internal/server/mcp_code_execution.go:124-126,249,473-499`; `internal/codescripts/codescripts.go:107-118,338-356`; `mcp_routing.go:849`; `mcp_code_scripts_test.go:75-78,299` | Agent ctx ["*"] script "gamma" with `alpha-SENTINEL.js` present → IsError, !contains SENTINEL/"Available scripts", byte-equal to empty-dir proxy; admin still enumerates. | P1 | scope-regression-suite (split out early, see §6) |
| FR01x-G2 | FR-012 (narrow golden exception) | `code_execution` description (`:52-53`) and `script` param text (`:73-74`) still advertise discovery-by-failed-call; pinned in 3 goldens; docs teach it; no "administrator" wording anywhere in file. Ship with G1 (else description is honest-but-false). | `mcp_code_execution.go:52-53,73-74`; `testdata/toolslist_goldens/{default_server.json:130,156; retrieve_tools_mode.json:130,156; code_execution_mode.json:10,36}`; `toolslist_snapshot_test.go:163-175,199-211`; `docs/code_execution/{overview.md:379-387,cookbook.md:141,troubleshooting.md:604-613,api-reference.md:591}` | `TestCodeExecutionDescriptions_EnumerationIsAdminOnly`; regenerate goldens (MCPPROXY_WRITE_TOOLSLIST_GOLDENS) + assertion that only `description` and `script.description` changed. | P2 | scope-regression-suite (early split) |
| FR01x-G3 | FR-012 (doc invariant) | `docs/features/agent-tokens.md` lacks the invariant sentence, covered-surface list (/mcp/all,/mcp/code,/mcp/call absent), retained-effects list, and custom-instructions/stored-scripts secrets warning. Only Spec-105 text is #1226's read_cache paragraph (:261-272); #1272 added Spec-106 text (:462-474). | grep of docs/ for invariant strings = 0; `docs/configuration.md:1244-1256` no warning | Optional docs-lint test (none exist in internal/) asserting the strings; else PR-review item. | P2 | scope-regression-suite |
| FR01x-G4 | FR-013/SC-001/SC-007 | No two-fixture differential oracle (a/b/a__b sentinel fixture, a-only fixture, normaliser, retrieve-oracle derivation, retained-effect fixtures). All shipped scope tests are single-fixture (hidden-vs-absent shape parity or content non-disclosure). Would fail on HEAD at US1.5 (fleet `total_indexed_tools`, `usage_summary`, `session_risk`). | `mcp_tail_log_scope_test.go:88-107`; `mcp_prompt_scope_test.go:270-283`; `mcp_read_cache_authz_test.go:84-93`; `profile_tool_test.go:233-257`; `mcp_direct_skew_test.go:42-49`; `mcp.go:1719,2007-2016,2023,2037-2041` | `scope_differential_test.go`: `newScopeFixture(t, full)`, `normalizeScopeResponse`, `runScopeScenario`; scenario funcs named by US id. | P1 | scope-regression-suite |
| FR01x-G5 | FR-014 | No credential-authenticated HTTP matrix: only Bearer agent-token requests through `mcpAuthMiddleware` are two initialize-only POSTs to `/mcp/p/<slug>`; every Spec-105 test injects AuthContext. Registration-level "n/a stays unavailable" IS pinned on all four surfaces (3 goldens + `direct_mode_builtins.json`) and mcp-go refuses unregistered names; missing = dispatch-level unknown-tool assertion over the wire per agent token. Coverage gap, not runtime defect for merged FRs. | `server.go:348-455,2671-2712`; `profile_integration_test.go:627-643,647-683,775-808`; `toolslist_direct_builtins_test.go:119,179`; `mcp_routing.go:924-981`; `unit-tests.yml:148` | `mintAgentToken(name, allowed, perms, pin)`; table surfaces × operations; 'yes' cells run differential oracle; 'n/a' cells assert -32602 == unregistered. Name without `E2E`; share one env per fixture (4s sleeps). | P1 | scope-regression-suite |
| FR01x-G6 | FR-011/SC-006 | No latency harness at all (no Benchmark in internal/server, no p95, no frozen 50-prompt set / 10-page cache entry, no 20+200 loop, no admin-vs-scoped delta, no benchstat/merge-base CI; `bench.yml` tag-only, non-blocking). Corpus loader exists. | `httpapi/preflight_bench_test.go:21-35` (only ceiling precedent); `mcp_routing_deferred_tokens_test.go:142-176`; `.github/workflows/bench.yml:3-6,20-22,38` | `TestScopeLatency_ScopedWithin20msOfAdmin` in-run delta + generous ceiling; merge-base bound = separate non-race job or documented manual measurement. | P2 | scope-regression-suite |
| FR01x-G7 (narrowed) | SC-007 | No test references any US id; US1.5, US1.8, US3.1-3.3 have no test (US2.6 only as stubbed jsruntime test). **Four** tests pin reversed behaviour (not three): `mcp_direct_skew_test.go:329-330`; `mcp_call_tool_target_tier_test.go:210,244-253`; `mcp_read_cache_authz_test.go:88`; `internal/cache/authorization_test.go:109-113`. SC-007's "five branches accepted only against it" is already moot — #1223-#1227 merged 2026-09-08, spec committed 2026-09-09 (#1232) → restate SC-007 as gate on FR-005/008/010 + list of assertions to invert. | as listed | Inventory table US-id → test name + `-run`; flip the four in the owning FR PRs. | P2 | scope-regression-suite |

---

## 2. Already satisfied per FR (do NOT re-implement)

**FR-005 (retrieve_tools)**
- Every index hit passes `indexedToolVisible` (scope → callability) before listing/droppedCount/locked entries: `mcp.go:1778,1784-1786`; `mcp_visibility.go:83-100`. `include_disabled` entries, remediation, zero-result `notice` (`:1979-1983`) already scoped (modulo G1 window).
- Quarantined/pending locked-entry discovery scoped via `serverDiscoverable` → `collectQuarantinedToolMatches` skips `!allowServer` (`mcp.go:1837,6640-6698`).
- `total` (`:1935`) and `filter_diagnostics` (`:1858-1881`, pure fn `mcp_annotations.go:173-188`) computed over the scoped window.
- `explain_tool` over scoped results only; hidden/nonexistent name identical payload (`mcp.go:2030,5553-5605`).
- Corpus selection preserved (`mcp.go:1705-1717`); deterministic ordering (`:1801-1811`); `isToolCallable` quarantine-blind (`:6455-6516`); one handler for /mcp, /mcp/call, /mcp/code (`:1573`); cache producer stamp from same authorization (`:1707,2084-2093`).

**FR-008 (direct publication)**
- Steady state: owner+tier from ONE record — catalog entry (`mcp_direct_catalog.go:213-223`, tier `:222` from upstream annotations), listing renders from it (`mcp_routing.go:169-183`), describe resolves from it (`mcp_describe_direct.go:75-89`), handler closes over it (`mcp_routing.go:390-392`).
- Dispatch identity at every seam correct (`mcp_routing.go:427-445`; pinned `mcp_direct_skew_test.go:325-333,479-489,570-599`).
- `__a` steady state: Lookup wins before parse test; `*` passes `CanAccessServer` (`auth/context.go:91-93`); admin short-circuit (`mcp_direct_scope.go:127`); a-only withheld (`:130`) and tools/call non-disclosing via mcp-go filter (`server.go:2080-2086`).
- Display-name collisions withheld everywhere (`mcp_direct_catalog.go:195-210`); publication serialized + failed discovery publishes non-nil empty catalog (`mcp_routing.go:109-124,1091-1094`).
- Admin listing served raw registry (`mcp_direct_scope.go:48-50`) = SC-005 baseline to preserve via stamp-and-strip.

**FR-010 (refusal shapes)**
- Precedence scope→tier on all three paths (`mcp.go:2262-2323`; `mcp_routing.go:422-455`; `jsruntime/runtime.go:387-410`).
- Retrieve scope refusal non-disclosing for restricted **unpinned** token (`mcp.go:2289`) and wildcard-pinned token (`:2270`).
- Tier denials on authorized servers = insufficient-permission on call_tool_* (`mcp.go:2319`) and nested (`runtime.go:407-408`).
- Direct hidden tool over HTTP outside seam → mcp-go `tool '<name>' not found` (`mcp_direct_scope.go:97-99,124-131`).
- describe_tool CHECK mode already one-policy on both surfaces (`internal/preflight/evaluator.go:216-241,617-682`; `mcp_describe_direct.go:204-210,232-236`); pinned by `TestDescribeToolCheck_ScopeSilenceIsByteIdentical`, `TestDescribeDirectCheck_SuggestionsNeverCrossTheVisibilityBoundary`.
- Suggestions never name a hidden candidate (`mcp_visibility.go:242`; `mcp_describe_direct.go:150`).
- tail_log (#1224) and upstream_servers config refusals form no existence oracle (`mcp.go:5717-5750,3370-3407`). Only one fleet-enumeration site remains (`mcp.go:2476`; `profile_tool.go:82` already filtered by #1225).

**FR-003/FR-004 (profiles)**
- FR-003 unpinned selectable predicate on set_profile (`profile_tool.go:89-101,188-210`); non-selectable ≡ nonexistent single format string (`:99-101`), present-vs-absent differential `profile_tool_test.go:237`; `available:` = selectable set (`:100`); effective intersection for pin/token dimensions (`:166-177` applied at `:80,82,108`); wildcard-agent empty-profile refusal (`profile_tool_test.go:447`).
- FR-004 deleted-pin URL does not enumerate (`server.go:2366`; `profile_integration_test.go:775`).
- Pinned set_profile mismatch text names only own pin + echoed slug (`profile_tool.go:67`).
- Admin unchanged everywhere; set_profile tool text untouched (goldens green).

**FR-001/FR-002 (cache)**
- Stamp captured at authorization time before upstream round-trip (`mcp.go:1708,2268,2940`; writes via `cacheStoreAs` `:2078,2711,3157`; `content_forward.go:145,166,267`).
- Superset gate per page for agent readers: kind, AllowedServers/Permissions coverage, pin equality, profile-as-server-set (`manager.go:231-242`; `authorization.go:78-149`); deleted pin revokes (`mcp_read_cache_authz_test.go:125-173`).
- Every redemption door shares `handleReadCache` (`mcp.go:1142,2838,6038,6177`; `mcp_routing.go:783`); REST installs agent AuthContext incl. pin (`httpapi/server.go:578-581`).
- Agent/user readers already refused nil-producer entries (`manager.go:233-238`; `authorization.go:96-98`).
- Refused reads leave stats/record untouched; same-authorization page byte-identical (tests at `authorization_test.go:123-186`). Status-class parity holds on both surfaces.

**FR-007 (logs)**
- Authorize-before-lookup + shared `tailLogNotFound` (`mcp.go:5741-5746,5771-5774`).
- No second agent-invokable named op (`internal/auth/server_ops.go:59-70`; `mcp.go:3399-3403`).
- Per-record writer stamp already written (`logger.go:385`) — child output, docker, oauth tee records all carry `{"server": "<raw>"}`.
- Container ownership label already exists (`instance.go:57-65`; applied `isolation.go:527-529`).
- Callback-handler records (start/request) are subject-bound (`config.go:1407,1023-1028`); only the stop path leaks.

**FR-009 (identity)**
- Retrieve dispatch: single `lookupExactToolAnnotations` feeds tier gate + intent validation (`mcp.go:2283,2317-2322,2358`); StateView holds raw names (`core/client.go:394-397`).
- Config denial on exact raw name on all paths (`tool_gate.go:118` → `mcp.go:6534` → `tool_quarantine.go:1753` → `config.go:2179-2194`; `mcp_direct_callability.go:181`; `mcp_code_execution.go:986`).
- Retrieve/nested never re-normalize; single-snapshot merged reader (`tool_gate.go:96-153,198-227`; `storage/manager.go:520`).
- Direct dispatch tier from immutable catalog entry; direct publication records raw name (`mcp_routing.go:391-393,440-450`; `mcp_direct_catalog.go:187,216`).
- Nested passes permission + policy gates before `client.CallTool` (`jsruntime/runtime.go:383-412`; `mcp_code_execution.go:257,980-1010,737`).
- Zero-upstream oracle infra exists (`mcp_call_tool_target_tier_test.go:335-411`); annotation-less→read rule exists in code (`intent.go:208-228`).
- Target-tier gate exact-match on all paths (G7 corrected).

**FR-011..014**
- Surface topology matches the FR-014 matrix (`server.go:2672-2705`; `mcp_routing.go:638-681`; three goldens + `direct_mode_builtins.json`); mcp-go refuses unregistered names (no dispatch fallback, `mcp_routing.go:924-981`).
- Goldens pass unregenerated on HEAD; enumerated-delta gate already whitelists `code_execution` (`toolslist_snapshot_test.go:199-211`).
- Nested script calls scope-checked (`mcp_code_execution.go:249-252`).
- Non-disclosing oracle pattern (tail_log/prompts/set_profile) and SC-002 counting oracle exist; 527-tool loader exists.

---

## 3. Refuted / narrowed claims

No gap had `verdict.refuted=true`. Six were narrowed — record so nobody re-raises the original framing:

| id | what was wrong | what stands |
|---|---|---|
| FR009-G7 | "`{read,destructive}` passes the target gate for a write tool" — **inverted**. `HasPermission("write")` is false → refused on retrieve (`mcp.go:2317`), direct (`mcp_routing.go:444-450`), nested. | Docs-only gap (agent-tokens.md cumulative claim is false; target-tier + defaulting rules undocumented). #1223 follow-up item 171 misstates mechanics. |
| FR003-G8 | "spec literal points to refusal" for pinned zero-reach pins — FR-003/004 predicate and Edge Case (1) are explicitly for UNPINNED tokens; Definitions retain "deleted pin → deny-all as today"; #1225 F2 deliberately locked "configured pin always selectable". | Real observable (pin existence) but an **open design decision**, not a spec violation. Record in spec; pick refuse per zero-interruption rule and document. |
| FR008-G6 | Sketched steady-state `__a` test **passes** on HEAD (catalog-first). | Fixture missing; the defect to pin is the seam (G2 mechanism). |
| FR008-G3 | Reverse flip is NOT "absence from previous catalog" (stale owner `a__b` fails CanAccessServer); `*` agent NOT affected. | Plain-addition + reverse-flip withholding for scoped tokens stands (P2, availability-only). |
| FR001-G7 | Held-call session/profile-narrowing fixtures would **pass** today (producer captured at dispatch). | Upgrade, internal-entry, recursive-child, pinned-REST body-parity fixtures fail; held-call = fixture-only. |
| FR001-G4 | Spec's named fixture (admin session {a} → child → pinned wildcard agent) does not discriminate (refused for two other reasons on HEAD). | Over-refusal gap stands; discriminating test = parent producer must read child. |
| FR010-G1 | HEAD text is `[b, a__b]` not `[a, b, a__b]`; b/a__b need `AddServerConfig` after PhaseReady, not storage-only seeding. | Enumeration leak stands. |
| FR010-G5/FR008-G5 | Line numbers off (`:424/:434/:328`). | Stands. |
| FR007-G1 | Case-only collision is FS-dependent; records already stamped (missing filter, not identity). | Stands. |
| FR01x-G5 | "unavailable stays unavailable" IS pinned at registration level on all four surfaces. | Missing = agent-token wire-level dispatch assertion + two-fixture oracle. |
| FR01x-G7 | Three pinned reversals → **four** (add `cache/authorization_test.go:109-113`); US1.2/US2.6 have tests (opposite / stubbed). | SC-007 already moot for #1223-#1227. |

---

## 4. Shared building blocks to reuse (from #1223-#1227 / Spec 099/102)

| block | file:line | used by |
|---|---|---|
| `serverInScope(authCtx, profileScope, name)` + `resolveActiveProfile(ctx)` (pin > URL > session, stale pin = deny-all) | `mcp_visibility.go:161-166`; `profile_resolver.go:118-164` | every new scoped computation (FR-005 search/count/usage/risk, FR-010 available-servers, FR-007 attribution) — never re-derive from AllowedServers |
| `serverDiscoverable` closure already built in the retrieve handler | `mcp.go:1752-1754` | FR-005 G1-G4 |
| `indexedToolVisible` / `toolIndexed` / `lookupIndexedTool` | `mcp_visibility.go:83-100,171-190` | FR-005 G2 stale-history exclusion; FR-010 G2 |
| `selectableProfileNames` / `callerVisibleServers` (`auth.IsScopedCaller` + `auth.CanEnumerateServer`) | `profile_tool.go:166-177,188-210`; `auth/context.go:240` | FR-004 profileMiddleware (key on IsScopedCaller, not AuthTypeAgent) |
| Single not-found constructor pattern: `tailLogNotFound` | `mcp.go:5717-5720` | FR-010 G1/G7, FR-001 G5, FR-004 uniform 404 |
| `visibleCorpus.notFoundResult` / `candidates()` (one-policy over authorized corpus) | `internal/preflight/evaluator.go:617-682` | FR-010 G2/G4 describe definition mode |
| `planDirectCheck` visible-entries suggestion corpus | `mcp_describe_direct.go:223-236` | FR-010 G3/G4 |
| Prompt `_meta` private-struct stamp + strip + positive built-in set (`aggregatedPromptStamp`, `stripAggregatedPromptServer`, `builtinPromptNames`, `authorizeAggregatedPromptServer`, `errPromptNotFound`) | `mcp_routing.go:1349-1351,1388`; `mcp_direct_scope.go:255-272` | FR-008 G1-G5 (stamp catalog entry into rendered tool Meta; populate `builtinDirectToolNames` from `buildDescribeToolTool().Name` at `mcp_routing.go:143-148`) |
| mcp-go value-copy filters (Meta reassignable per response); call-time `passesToolFilters` | mcp-go `server/server.go:1920,1961,2080-2086` | FR-008 strip design; FR-010 G6 (keep scope in filter, tier out) |
| `lookupToolApproval` + `Manager.GetToolApprovals(server, names...)` single-snapshot reader; `evaluateExactToolGate`/`isExactToolCallable` split | `tool_gate.go:86-153,198-227`; `storage/manager.go:520`; `bbolt.go:403` | FR-009 G3 (direct/preflight readers), G5 |
| `tierForAnnotations`, `lookupExactToolAnnotations`, `normalizeServerTool`, `policyRefusal`, `emitActivityPolicyDecision(..."blocked"...)` | `mcp_code_execution.go:1260,980`; `mcp.go:6768`; `mcp_visibility.go:145` | FR-009 G4 |
| `cache.Authorization`, `StoreAs`/`GetRecordsAs` guard closure, `cacheAuthorizationWith`, `producerCacheStore`/`cacheStoreAs` | `internal/cache/authorization.go`; `manager.go:108-245`; `cache_authz.go:20-86` | FR-001/002 all |
| Server `--label com.mcpproxy.server=<raw>` | `core/instance.go:57-65` | FR-007 G5 |
| Writer stamp `logger.With(zap.String("server", name))` | `logs/logger.go:385` | FR-007 G1-G3 reader |
| **Fixtures:** `createTestMCPProxyServer` (`mcp_auth_scope_test.go:25`), `createTestProxyWithRuntime`, `buildMCPProxyWithActivation` (`mcp_activation_test.go:40`), `seedTargetTierServer`/`readOnlyAgentCtx`/`startCountingTargetTierUpstream`/`watchPolicyDecisions` (`mcp_call_tool_target_tier_test.go:35-411`), `newSkewFixture`/`rebuildPaused`/`listed`/`registeredHandler` (`mcp_direct_skew_test.go:42-128`), `newTailLogScopeProxy`/`assertTailLogHidden` (`mcp_tail_log_scope_test.go:30-117`), `newProfileTestEnv`/`mintPinnedToken` (`profile_integration_test.go:34,627` — generalise to `mintAgentToken`), `newSetProfileTestServer`/`setProfileScopedCtx` (`profile_tool_test.go`), `readCacheAs`/`cacheKeyRE`/`setTruncateLimit` (`mcp_read_cache_authz_test.go:30`; `mcp_retrieve_tools_truncation_test.go:32,38`), `setupQuarantineRuntime` (`tool_quarantine_test.go:19`), `newSigWarmRuntime` (`sigcache_warm_test.go`), `setupTestDB`+`Peek` (`cache/manager_test.go:14`), fake docker (`connection_docker_integration_test.go:39,92-95`), observer cores (`oauth/callback_redact_test.go:42`), `loadDeferredLargeCorpus` (`mcp_routing_deferred_tokens_test.go:142`) |
| Agent contexts: `auth.WithAuthContext(ctx, &auth.AuthContext{Type: AuthTypeAgent, AllowedServers: ["*"]|["a"], Permissions: ..., ProfilePin: ...})`; admin `auth.AdminContext()` | `auth/context.go:84-111` | all (empty AllowedServers = deny-all!) |

---

## 5. Codex diff disposition (`codex105.diff`, 931 lines vs `6b005e949`; applies textually to HEAD)

| hunk group | FR | disposition | required changes |
|---|---|---|---|
| 1. `internal/cache/{authorization,manager,models}.go` + tests (Version, `valid()`, `errInvalidProvenance`, getGuarded commit-then-refuse, admin-first `CouldHaveProduced`, `ReadCacheResponse.Producer`) | FR-001/002 | **port with changes** | keep `len(reader.ProfileServers)==0 → false` ahead of admin shortcut; stamp internal writers (`runtime.go:2226`, `guesser.go:349`) as `CallerKindInternal` = refuse-keep, reserve delete-on-refusal for nil/unrecognised (else guessable keys = eviction DoS); Version-0 stamped entries become legacy — upgrade fixture must cover nil AND v0; add reopen-bbolt assertion; goimports |
| 2. `mcp.go` handleReadCache (collapse errors, child stamped with parent) + `mcp_read_cache_authz_test.go`, `mcp_call_tool_direct_test.go` | FR-001 | **port with changes** | collapse only ErrUnauthorizedRead/not-found/expired (keep real BBolt errors distinct); document parent-stamp simplification; fix import order |
| 3. `config/tool_identity.go`, `config.go` RawName, `core/client.go`, `index/bleve.go` | FR-009 | **port with changes** | derive RawName on index READ from canonical `Name` (`TrimPrefix(canonical, server+":")`), not stored `tool_name` (collapsed for old docs); add index rebuild trigger (schema-version precedent `tool_quarantine.go:437`) since stale `srv:erase` docIDs persist |
| 4. `index/manager.go` `SearchScoped` + `mcp.go:1720` routing | FR-005 G1 | **port verbatim** (+ fixes) | gate on `authCtx != nil && !authCtx.IsAdmin()` not `Type==AuthTypeAgent`; decide profile-scoped-admin-on-fallback explicitly; consider full `indexedToolVisible` pre-cut; measure vs FR-011 (bleve `server_name` disjunction is the cheaper follow-up) |
| 5. `mcp.go` scoped counts/usage/risk, `mcp_annotations.go` `analyzeSessionRiskScoped`, `mcp_visibility.go` `scopedIndexedToolCount` | FR-005 G2-G4 | **port with changes** | `analyzeSessionRiskScoped` verbatim; count with scope-only predicate (not callability) and keep `getIndexedToolCount()` for admins (SC-005); use facet counts; usage: keep admin on `GetToolStats(10)`, add index-presence check for stale history; rename shadowed `visible`; `scopedIndexedToolCount` depends on `RawToolName` — pass `tool.Name` through `normalizeServerTool` instead if shipped before hunk 3 |
| 6. `runtime/lifecycle.go`, `tool_quarantine.go` (RawToolName in producers) | FR-009 G1/G2 | **port verbatim** | resolve `extractToolName` (keep for 3 test files or delete+fix); must ship with hunk 3 |
| 7. `tool_gate.go` exact-only reader (deletes merge) | FR-009 G5 | **discard** | regresses: not-found → implicit-approved makes `ns:erase` callable until next discovery; orphans `mergeApprovalRecords`/`approvalLockRank`; breaks 5 #1223 tests. Redesign: exact wins; snapshot-present+no-record under active quarantine gate → pending; legacy collapsed record approves only its own raw name; one-shot migration |
| 8. `mcp_direct_catalog.go` (remove parse→Builtin), `mcp_direct_scope.go` (remove admin early return, NoCatalog → continue, prompt filter enforce guard) | FR-008 G2 | **discard as-is; re-port** | prerequisite never done: populate `builtinDirectToolNames` from `withDirectBuiltins` constructors; then remove parse fallback + admin early-return (needed for strip-for-all); keep NoCatalog window for admins; adds no identity stamp so does nothing for G1/G3/G4 |
| 9. `mcp_routing.go` renderDirectTools `serverName == "" \|\| promptName == ""` | FR-006/FR-008 G7 | **discard; re-implement** | does not compile (identifiers out of scope); belongs at `mcp_routing.go:1322` (prompt aggregation) + `prompts/get` at `mcp.go:900`; tool analogue = empty raw name check in `buildDirectCatalog` |
| 10. `profile_tool.go` (`effectiveProfileServers`, pin-branch reach check), `server.go:2321` agent gate, `profile_pin_enforcement_test.go`, `profile_scope105_test.go` | FR-003/004 G1-G8 | **port with light changes** | key on `auth.IsScopedCaller`; dedupe slug parsing; keep pin-branch change only if G8 decision = refuse; update `TestProfile_PinnedTokenURLEnforcement`, `TestProfile_DeletedPinDoesNotEnumerateProfiles`, `_PinnedTokenClearIntersectsAllowedServers`, `_PinnedTokenSelectsDisjointPin`; update `docs/features/profiles.md:70`; do not short-circuit 'no profiles' before the gate |
| (absent) | FR-007, FR-010, FR-011-014 | n/a | nothing in the diff; write from scratch. Sentinel-in-name + same-body-across-slugs loop in `profile_scope105_test.go` is a usable idea only |

Port order (assessment): 3+6 → 7 redesigned → 1+2 → 4+5 → 10 → 8 re-done → 9.

---

## 6. Suggested PR split (one per roadmap task; dependency order)

| # | PR / roadmap id | FRs | gaps | files touched | depends on | collision risk |
|---|---|---|---|---|---|---|
| A | **scope-target-identity-producers** | FR-009 | FR009-G1..G7 | `internal/config/{config.go,tool_identity.go(new)}`, `internal/upstream/core/client.go:394`, `internal/index/bleve.go:158-183,356-372,433`, `internal/runtime/{lifecycle.go:673-790,975; tool_quarantine.go:443-616,1216-1229}`, `internal/server/{tool_gate.go:96-227, mcp_direct_callability.go:253, preflight_glue.go:404, mcp_code_execution.go:1251-1268,980-1000, mcp.go:2283-2323}`, `internal/preflight/classify.go:98`, `docs/features/agent-tokens.md:140`, tests (`mcp_call_tool_target_tier_test.go:210,225,474` flipped; `tool_quarantine_test.go`; `mcp_routing_test.go:306-447` rebuilt on real fixture) | — | **B** (mcp.go different region), **C** (`mcp_visibility.go` if codex hunk 5's `RawToolName` dependency is kept — avoid), **F** (`mcp_direct_callability.go:253` — A changes the reader, F changes what pair is fed; coordinate), index rebuild trigger affects all index tests |
| B | **scope-cache-legacy-invalidation** | FR-001/002 | FR001-G1..G7 | `internal/cache/{authorization.go:77-95, manager.go:108-245, models.go:26-43}`, `internal/server/{cache_authz.go, mcp.go:5613-5690, content_forward.go:256}`, `internal/runtime/runtime.go:2226`, `internal/experiments/guesser.go:349`, tests (`cache/authorization_test.go:65-72,93-154` flipped; `manager_test.go:271`; `mcp_read_cache_authz_test.go:86,171`; `mcp_call_tool_direct_test.go:35-48`) | — | **A/C/E/G** all touch `mcp.go` in other regions (5613-5690 is B-only); low |
| C | **scope-retrieve-tools** | FR-005 | FR005-G1..G5 | `internal/index/manager.go:105` (SearchScoped), `internal/index/bleve.go:458` (facet counts — A also edits bleve.go: sequence A first), `internal/server/{mcp.go:1705-2043, mcp_annotations.go:29, mcp_visibility.go}`, `internal/storage/manager.go:424` (unlimited/filterable stats), tests (`TestRetrieveTools_ScopeOracle`, goldens must stay byte-exact) | A (bleve.go ordering only) | **G** (`mcp_visibility.go` — C adds count primitive, G reorders `toolVisibleToSession :51-60`; different funcs), **H** (retrieve oracle consumes C) |
| D | **scope-selectable-profile-predicate** | FR-003/004 | FR003-G1..G8 | `internal/server/{profile_tool.go:72-108,188-210, server.go:2320-2384}`, `docs/features/profiles.md:70-72`, tests (`profile_integration_test.go:201-233,647-703,775`; `profile_tool_test.go:344,356,397`; `profile_pin_enforcement_test.go:150,160`; new `mintScopedToken`) | — | **H** (`mintAgentToken` generalisation — D should introduce it); `server.go` untouched by others except none; low |
| E | **scope-log-attribution** | FR-007 | FR007-G1..G6 | `internal/logs/logger.go:321-547` (attributed reader; consider dedicated owner key at :385), `internal/server/mcp.go:5787-5803`, `internal/oauth/config.go:1686-1735` (stop via `server.logger`), `internal/upstream/core/docker.go:349-431,501-582` (label filter + `^mcpproxy-<san>-[a-z0-9]{4}$`), tests (`mcp_tail_log_scope_test.go:63-64,110-117` rewritten to stamped writers; `mcp_secret_redaction_test.go:288`; new logs/oauth/docker tests) | — | **B** (mcp.go other region); none else; low. Admin/CLI readers (`upstream_cmd.go:826`) stay whole-file; the REST reader (`server.go:3481`) also stays whole-file but is NOT admin-only — see §8 |
| F | **scope-direct-publication** | FR-008 (+US3.4 refusal text) | FR008-G1..G7 | `internal/server/{mcp_routing.go:143-148,156-311,390-465,1322-1326, mcp_direct_catalog.go:151-224,374-423, mcp_direct_scope.go:40-143, mcp_direct_callability.go:49-92, mcp_describe_direct.go:45-103}`, tests (`mcp_direct_skew_test.go:195-224,316-332,459-489` inverted; `mcp_direct_catalog_publish_test.go:65-101,155`; `mcp_direct_catalog_test.go:177-235`; new `__a`, deferred, reverse-flip, empty-raw-name) | A (callability reader) | **G** (both edit `mcp_direct_scope.go` filter chain and `mcp_direct_catalog.go` — G needs F's stamp + filter split; sequence F before G); `toolSetFingerprint` / `wireEntry` see the Meta stamp — route via filter chain |
| G | **scope-refusal-shapes** | FR-010 | FR010-G1..G7 (G5 delivered by F) | `internal/server/{mcp.go:2262-2293,2473-2495, mcp_visibility.go:51-72, mcp_describe_tool.go:123-160, mcp_describe_direct.go:54-156, mcp_direct_catalog.go:227-268 (shadow canonical map), mcp_direct_scope.go:115-143 + mcp_routing.go:958-961 (tier out of WithToolFilter → list-only path), mcp_code_execution.go:1198-1229}`, `internal/jsruntime/runtime.go:383-410`, tests (`mcp_auth_scope_test.go:84`; `describe_plain_corpus_test.go` delta if any; `TestDescribeDirect_DisplayAndCanonicalNamespaceOverlap` admin control kept) | C (scoped-count/search primitive for describe not-found policy per roadmap:444), F | **F** as above; **A** (`mcp_code_execution.go` — A edits 980-1000/1251-1268, G edits 1198-1229; adjacent); **B** none |
| H0 | **scope-regression-suite (part 1: FR-012 stored scripts)** — split early, independent | FR-012 | FR01x-G1..G3 | `internal/server/mcp_code_execution.go:50-74,490-497`, `internal/codescripts/codescripts.go:107-118,338-356` (or caller-kind branch in server), 3 goldens regenerated (+ narrow-diff assertion in `toolslist_snapshot_test.go`), `docs/code_execution/{overview,cookbook,troubleshooting,api-reference}.md`, `docs/features/agent-tokens.md` (invariant, surfaces, retained effects, warning), tests (`mcp_code_scripts_test.go:299` kept as admin control + agent case) | — | **A** (`mcp_code_execution.go` — H0 edits 50-74/490-497; A edits 980+/1251+; low) |
| H1 | **scope-regression-suite (part 2: harness)** | FR-011/013/014, SC-007 | FR01x-G4..G7 | new `internal/server/scope_differential_test.go`, `scope_http_matrix_test.go` (no `E2E` in name), `scope_latency_test.go`; `profile_integration_test.go` env reuse; inventory table US-id → test | C, F, G (US1.5/1.8/3.x fail until landed), D (mintAgentToken) | test-only; will surface any remaining pinned-reversal test not yet flipped |

Recommended merge order: **A → B ∥ D ∥ E ∥ H0 (parallel, disjoint) → C → F → G → H1**. Every PR: `go test -race -count=1 -skip 'E2E|Binary|MCPProtocol|TestInfoEndpoint|TestGracefulShutdownNoPanic|TestSocketInfoEndpoint' ./internal/server/...`, `/opt/homebrew/bin/golangci-lint run --config .github/.golangci.yml ./...`, `git diff -- internal/server/testdata` empty (except H0), opencode gpt-6-astra review (cap 10 rounds), "Follow-ups / Spec 105 gaps" checklist in body, `docs.mcpproxy.app` links not repo paths.

---

## 7. Risks / gotchas

**Frozen goldens (FR-012).** `testdata/{retrieve_full_default,retrieve_full_stats,tools_list_prefeature,direct_full_prefeature}.golden.json` + `toolslist_goldens/*` + `describe_plain_corpus_test.go` are byte-exact. Only H0 may regenerate, limited to `code_execution.description` + `script.description`. `retrieve_full_stats` pins `usage_summary:{top_tools:[]}` non-nil (admin path must stay identical, nil-vs-[] matters). `direct_full_prefeature` is an ADMIN raw-JSON-RPC capture carrying `_meta.anthropic/maxResultSizeChars` → any FR-008 Meta stamp must be stripped for every caller (drop the `mcp_direct_scope.go:48-50` early return) restoring the upstream Meta pointer verbatim. Broad `gofmt -w` churns unrelated files.

**Spec 085 predicate.** `isToolCallable`/`isExactToolCallable`/`indexedToolVisible` is the SEARCH filter and must stay quarantine-blind. FR-005 G1 = oversize search + `serverInScope`, never quarantine gates; FR-009 G5's "no record → pending" belongs in dispatch consumers / `ClassifyTool` input, not the shared predicate; FR-010 G2 reorder belongs in `toolVisibleToSession` (describe-only).

**Agent-token fixtures.** Empty `AllowedServers` = DENY-ALL under `CanAccessServer`/`serverInScope`/`callerVisibleServers` but UNRESTRICTED under `preflight.ResolveScope`. Unrestricted fixtures need `["*"]` (`mintPinnedToken` hardcodes it + PermRead only — generalise). `HasPermission` is exact-match. Stale `ProfilePin` → deny-all (`profile_resolver.go:132-141`).

**Tests that pin pre-105 behaviour — invert, don't delete:** `mcp_direct_skew_test.go:318,328-332,477` (F); `mcp_call_tool_target_tier_test.go:210-223,244-253,474` (A); `mcp_read_cache_authz_test.go:86,171`, `cache/authorization_test.go:65-72,93-121,123-154`, `cache/manager_test.go:271` (absence), `mcp_call_tool_direct_test.go:35-48` (B); `profile_integration_test.go:667-670,681-701,201-233,799`, `profile_tool_test.go:344/356,397`, `profile_pin_enforcement_test.go:150,160` (D); `mcp_tail_log_scope_test.go:63-64,110-117`, `mcp_secret_redaction_test.go:288` (E); `mcp_direct_catalog_publish_test.go:95-101,155`, `mcp_direct_catalog_test.go:177-235`, `mcp_direct_skew_test.go:195-224` (F); `TestExpiredRecords`. Keep as admin controls: `TestCodeExecution_ScriptNotFoundListsAvailable`, `TestDescribeDirect_DisplayAndCanonicalNamespaceOverlap`, `TestTailLog_AdminUnchanged`, `TestHandleSetProfile_AdminUnchanged`, `TestProfile_404UnknownSlug/404NoProfiles`.

**SC-005 admin parity per FR.** Every scoped computation short-circuits `authCtx == nil || authCtx.IsAdmin()` with nil profile scope. Named admin exceptions only: no-identity withholding (FR-006/008), legacy + fresh-internal cache (FR-002), config-denied `a:ns:erase` + distinct index entries (FR-009), container ownership + OAuth routing (FR-007), code_execution wording (FR-012). Decide and document: profile-scoped admin on retrieve fallback (FR-005), kind-first cache ordering (FR-001 G6 — loosens #1226), profile-scoped admin tail_log (#1224 R1-1), `/mcp/p` refusal text for admins (FR-004 keeps three branches).

**mcp-go v1.0.0 mechanics.** Every `WithToolFilter` re-runs at tools/call (`server.go:2080-2086`): FR-010 G6 cannot be fixed in the handler — keep scope in the filter, take tier/callability out to a list-only path; moving scope out reopens listing disclosure. Handler errors → -32603, filter refusal → -32602, isError result = third envelope: assert message parity + zero upstream calls, not code parity (#1227 R1-4). Filters get value copies — reassigning `tool.Meta` never mutates the registry.

**Durable invalidation (B).** bbolt rolls back on non-nil return from `Update`: delete with tx fn returning nil, surface refusal outside; mutate `m.stats` only on the committed path. Internal entries must refuse WITHOUT evicting (`runtime.go:2160` Peek, `guesser.go:258` Get are the legitimate readers) or guessable keys become an eviction DoS. Unified body keeps `cache key not found` substring; activity log (`mcp.go:5661`) may keep the real reason.

**Identity migration (A).** Producer change is a data-migration event: collapsed BBolt records + `a:erase` docIDs for raw `ns:erase` persist; every discovery on HEAD DELETES exact-name records (`lifecycle.go:783`) — ship producer before reader; index rebuild trigger needed (schema-version precedent `tool_quarantine.go:437`); annotations are NOT in the approval hash (`tool_quarantine.go:36-55`). Undiscovered→refuse crosses `ToolAnnotationFunc` (string contract cannot express "refuse") — keep admin + unknown-server fail-open (`mcp_code_execution.go:990-1000`); in-process fixtures registering an upstream without StateView entry (`mcp_call_tool_trim_test.go:57`) will start refusing.

**Direct publication (F).** Stamp catalog entry into rendered Meta as a private struct (unforgeable), read stamp in both filters, catalog only for unstamped; unstamped non-built-in → withheld for everyone; `builtinDirectToolNames` from `buildDescribeToolTool().Name` (never hardcode). Spec 102 SC-007 listing/describe parity: describe resolves from catalog — keep skew in the documented safe direction only. `toolSetFingerprint`/`wireEntry`/`TestDirectModes_SetIdentity` marshal the raw registry and will see the stamp.

**Logs (E).** Console format `ts | LEVEL | caller | msg | {json}`; child messages are unescaped and may contain ` | `/JSON — take the LAST ` | ` segment and the FIRST `server` key (duplicate keys `{"server":"a_b","server":"a/b"}`; `encoding/json` keeps LAST). Prefer a dedicated unforgeable key (`app.mcpproxy/owner`) at `logger.go:385`; then pre-key records are non-attributable by rule. Two lumberjack sinks on one file corrupt each other (probe) — document as retained/known. Case-only collision is FS-dependent; assert via attributed reader on one shared file. Close returned `io.Closer` in fixtures (CI 'directory not empty'). OAuth: don't call `adoptLoggerLocked` from stop (side effect re-installs manager logger); keep nil-logger signature for tests. Docker: `newDockerCmd` resolves via `shellwrap.ResolveDockerPath` (cached) → tests need `SetWellKnownDockerPathsForTest` + `ResetDockerPathCacheForTest`; label filter drops pre-label/user-`--name` containers (extractContainerNameFromArgs path handles `--name` separately); belt-and-braces regex `^mcpproxy-<san>-[a-z0-9]{4}$` still conflates `a/b` and `a-b` (only label disambiguates); scanner looks up by same prefix (MCP-2123, out of scope — note in PR).

**Profiles (D).** Gate on `auth.IsScopedCaller` (server-edition `AuthTypeUser` is scoped per #1225); unauthenticated `/mcp` = `AnonymousContext` (admin-typed, `server.go:382`); uniform refusal must be produced by ONE code path so timing class does not vary; don't short-circuit 'no profiles' before it. `profile_tool.go:handleSetProfile` was already a #1225/#1227 conflict hotspot — keep footprint tight. G8 contract decision recorded in PR body.

**Retrieve (C).** Exhaustive `Size=documentCount` loads 7 stored fields per hit — measure against FR-011 (20 ms scoped bound on 527 tools); a capped oversize does NOT in general equal exhaustive top-K. `TestFilterDiagnostics_WindowUsesNormalizedLimit` (cut to exactly `limit` after filtering), `TestSurfaceIsolation_RetrieveTools*` (toon byte parity), `TestRetrieveTools_TruncatedPayloadReadableViaReadCache` (producer stamp at `mcp.go:1707`) must keep passing. `session_risk` emitted only with runtime (`mcp.go:2010`).

**Test runtime.** `go test ./internal/server` hangs to the 7m panic without the CI `-skip` regex; naming an HTTP matrix test `*E2E*` skips it in CI; `Detector.Scan` ~23x slower under `-race` (keep payloads small); `startCountingTargetTierUpstream` needs `MCPPROXY_DISABLE_OAUTH=true` + free loopback port; `newProfileTestEnv` sleeps 4s/env; `go build -tags server` must use `-o /dev/null` (clobbers `./mcpproxy`).

**FR-011.** No merge-base infra (bench.yml tag-only, non-blocking; `-race` inflates ~20x) — ship in-run scoped-vs-admin ≤20 ms delta + ceiling; cross-revision bound in a separate non-race job or documented manual measurement.

**Spec text fixes to fold in** (no code): FR-008 name the empty-raw-tool-name analogue (G7); FR-003/004 record the pinned zero-reach decision (G8); SC-007 restate as gate on FR-005/008/010 + list of assertions to invert (G7); FR-009 note exception (2) admin applicability vs "scoped callers" wording (G4); FR-002 anonymous-caller grey zone vs `spec.md:36`. Code comments in #1223-#1227 cite Spec 104 FR-016x — keep that lineage style until 105 merges.

## 8. Follow-ups / Spec 105 gaps (out of this spec's scope — recorded, not fixed)

**REST `GET /api/v1/servers/{id}/logs` serves the whole shared file to scoped agent tokens (PR E critique r1, findings C1.1 / C2.1).** `internal/httpapi/server.go:505-508` admits `mcp_agt_` tokens on REST and `:828` mounts `/logs` inside `scopedServerSubtree` (`scope_subtree.go:164-194`), which gates only the server NAME; the handler (`:3745`) calls `Server.GetServerLogs` (`internal/server/server.go:3454-3527`), a raw last-N-lines read with no attribution. So a token allowed `{a_b}` gets hidden `a/b`'s records, pre-105 callback records naming `a/b`'s bind host/port, and legacy foreign-container ids — the exact bytes `handleTailLog` withholds under FR-007. Spec 105 scopes the REST management API out (`spec.md` "Transport and caller scope"), and PR E's plan keeps REST/CLI whole-file, so PR E documents the gap (`docs/features/agent-tokens.md` no longer calls the endpoint an administrator reader) and does not change it. `POST /api/v1/diagnostics/fix` `show_last_server_logs` (`diagnostics_fixers.go:94`) reads whole-file too but sits behind `requireServerOp` (`server.go:887`), so agents are blocked there. *Proposed fix (REST entitlement work, #1166):* when `auth.IsScopedCaller(ctx)`, route the handler through `logs.ReadUpstreamServerLogTailAttributed` — the controller interface (`httpapi/server.go:146`) lacks `ctx`, so add a ctx-taking variant or an optional capability assertion as `EmitActiveProfileChanged` does — and add the `a_b`-token / hidden-`a/b` cell to `scope_round9_test.go` (the existing `TestServerLogs_ScopedTokenNeverSeesAnotherServersStderr` covers only the name gate).

**Whole-file reader line cap (PR E critique r1, findings C1.2 / C2.10).** `logs.ReadUpstreamServerLogTail` (`internal/logs/logger.go:527-547`) uses bufio.Scanner's 64 KiB default, while `launcher.pumpLines` allows a 1 MiB child line, so one >64 KiB line makes the admin `tail_log`, REST and CLI reads fail with "token too long" while the scoped read (which skips over-long lines, 1 MiB cap) succeeds — a caller-kind divergence in the wrong direction. Pre-existing administrator bug; left untouched in PR E because `logger.go` being untouched is what validates the admin byte-parity oracle (`TestTailLog_CollidingLogFile_AdminWholeFileUnchanged`). Fix separately: give the whole-file reader the same skip-over-long behaviour (and `GetServerLogs` in `internal/server/server.go:3510`).

**REST replay dispatches with no tool gate (pre-existing; PR A round-3 review finding 3).** `POST /api/v1/tool-calls/{id}/replay` (`internal/httpapi/server.go` route registration ~`:904`, handler `handleReplayToolCall` ~`:4978-4992`) only asks `canSeeServer` for a scoped caller, then `Runtime.ReplayToolCall` (`internal/runtime/runtime.go` ~`:1350-1437`) dispatches `client.CallTool` directly. Nothing in that path consults the tool gate the MCP surfaces share (`evaluateExactToolGate`: quarantine, pending/changed lock, user Disabled, config deny, no-record-under-active-gate), resolves the identity (`resolveExactToolIdentity`: undiscovered / stale name), or checks the caller's tier against the target tool (`tierForAnnotations` + `HasPermission`). Consequences: a recorded call to a tool that is now pending / changed / user-disabled / config-denied — including a namespaced tool with no exact record — re-executes upstream; and a **read-only** agent token with server scope can replay a recorded **destructive** call, which is exactly the target-tier bypass FR-009 closes on the MCP surfaces. Spec 105 scopes the REST management API out (`spec.md` "Transport and caller scope": "The REST management API keeps its existing policy … and is out of scope here"; the REST entitlement work is #1166's), so PR A does not change replay. *Proposed fix (separate spec/PR):* before `client.CallTool`, route replay through `resolveExactToolIdentity` (refuse `Unresolved()`), `evaluateExactToolGate` (refuse `!callable()`, answer quarantine / lock / block as dispatch does) and `tierForAnnotations` + `HasPermission` for scoped callers — or dispatch through `handleCallToolVariant` with the recorded variant so the REST door reuses the whole MCP funnel; add the read-only-token-replays-destructive cell and the pending/changed/disabled/no-record cells with a counting upstream (zero upstream calls). Note the gate primitives live in `internal/server`; the replay lives in `internal/runtime` + `internal/httpapi`, so the fix either moves a gate reader into a shared package or has httpapi call the MCP proxy's funnel.
