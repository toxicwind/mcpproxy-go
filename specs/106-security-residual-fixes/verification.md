# Verification: Residual Security Fixes

Verified locally on 2026-09-12 from the isolated worktree branch `codex/106-security-residual-fixes`.

## Real personal-edition instance

Built the personal binary and a real stdio MCP fixture, then started MCPProxy on `127.0.0.1:48765` with an isolated BBolt database and Unix socket.

- Executed `code_execution` through the running daemon with a nested `fixture.echo` response containing 9,000 benign characters before a synthetic AWS key, plus a synthetic GitHub token generated by the parent script.
- `activity list --sensitive-data --output json` returned both the parent `internal_tool_call` and child `tool_call` records.
- The parent reported `aws_access_key` and `github_pat` in both arguments/response detection locations. The nested record reported the AWS key even though its stored response was truncated.
- Stored arguments and responses were redacted; the detector reported `truncated:false` for the full scan source.
- Default table, JSON, and YAML status output showed the synthetic API key only as `synt****pace` and emitted no credential-bearing Web UI URL.
- `status --web-url` exposed the credential only on explicit request and encoded reserved characters as `%23`, `%3F`, `%26`, and `%25`, preserving the key as one query value.

## Real server-edition instance

Built with `-tags server`, seeded three real users and two owner-scoped tokens with the same opaque name `release/保安 %25`, and started MCPProxy on `127.0.0.1:48766` with isolated configuration and storage.

- `GET /api/v1/admin/tokens` returned both owners and safe metadata, without raw tokens or `token_hash`.
- `POST /api/v1/admin/users/{owner}/tokens/revoke` with the opaque name returned HTTP 200.
- The revoked owner's next authenticated request returned HTTP 401; the other owner's same-named token remained valid and returned HTTP 200.
- The admin sharing route committed `shared:false`; the still-valid token's next request returned an empty server list and zero aggregate counts.
- After restarting the server-edition process, the revoked token still returned HTTP 401 and the other token still had no entitlement to the unshared server.

## Automated checks

- `go test -v -race -timeout 20m -skip 'E2E|Binary|MCPProtocol|TestInfoEndpoint|TestGracefulShutdownNoPanic|TestSocketInfoEndpoint' ./internal/...` — passed.
- `go test -race -tags server -timeout 20m ./internal/serveredition/... ./internal/config/... ./internal/oauth/... ./internal/storage` — passed.
- `go test -race -timeout 10m ./cmd/mcpproxy` — passed.
- `go test -race -tags server -timeout 10m ./cmd/mcpproxy` — passed.
- `golangci-lint run --config .github/.golangci.yml --timeout=10m ./...` — zero issues.
- Personal and server-edition binaries built successfully and were the binaries used in the real-instance checks.
- `git diff --check` and `gofmt` verification — passed.

## Cross-review

OpenCode 1.18.29 did not expose the requested exact `claude-fable-5.1` model identifier, so the closest available Claude Fable model, `github-copilot/claude-fable-5`, performed the required cross-review. It found two actionable gaps: stale scope on already-open SSE streams and omission of full long error text from nested detection. Both were fixed and regression-tested.

OpenCode `github-copilot/gpt-6-astra` then reviewed the full security diff. Its findings about stale admission-gate publication, `AuthBroker` preservation, and initial SSE status revalidation were fixed. The final focused Astra review of the live-instance URL-encoding correction returned `CLEAN`.
