# Local Verification
Use an isolated data directory, synthetic key, loopback-only unused port and deterministic local MCP upstream. Build personal and server binaries from each final tree. Initialize MCP sessions; run scripts with secret-bearing input, generated output and a nested response exceeding 8 KiB. Poll activity metadata with a bounded deadline. Compare admin and scoped SSE subscribers for authorized/hidden/anonymous detection events. Compare disabled/quarantined statistics totals. Capture default JSON/text/YAML status output and explicit reveal modes. Seed two local server-edition users and signed sessions through the test harness, mint same-named tokens, revoke one through HTTP and retry both, then unshare an upstream and retry the surviving credential before and after restart. Record exact commands and outcomes in verification.md. No production configuration or processes are changed.
